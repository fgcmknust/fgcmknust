{
  "name": "fgcm-church",
  "version": "1.0.0",
  "description": "Full Gospel Campus Ministry — Premium Church Web App",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "npx wrangler pages dev ./dist --live-reload"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
